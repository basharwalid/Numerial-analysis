import 'package:flutter/material.dart';

class gaussEliminationResultScreen extends StatelessWidget {

  static const String routeName = "gauss elimination result screen";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(
          "hello"
        ),
      ),
    );
  }
}
